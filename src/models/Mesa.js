class Mesa {
  constructor(id, numero, capacidade, localizacao) {
    this.id = id;
    this.numero = numero;
    this.capacidade = capacidade;
    this.localizacao = localizacao;
  }

  create() {}
  read() {}
  update() {}
  delete() {}
}

module.exports = Mesa;